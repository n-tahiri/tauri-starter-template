import { heroui } from "@heroui/react";
export default heroui({
  themes: {
    dark: {
      extend: "dark",
      colors: {
        background: "#19181aff",
        foreground: "#ffffff",
      },
    },
  },
});
